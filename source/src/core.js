/**
 * BCS HQ Tracker - core.js
 *
 * @author Zac Belado pixelgeek@gmail.com
 *
 * @desc The core application logic for the BCS HQ Tracker application Will include the other JS classes we need for the app
 *
 */

// add some event listeners
document.addEventListener('DOMContentLoaded', documentLoaded);

/**
 * @function documentLoaded
 * called when the document is finished loading. We'll use this as a hook to build the UI
 * and do some initial setup for the user
 */
 
 function documentLoaded () {
     
 }